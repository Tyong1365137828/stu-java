JDBC编程的六步骤：
	1、注册驱动；
		通知Java程序我们即将要连接哪个品牌的数据库
		
	2、获取数据库连接；
		此时Java进程和mysql进程之间的通道就打开了
	
	3、获取数据库操作对象；
		这个对象很重要，是用这个对象执行SQL的
	
	4、执行SQL语句；
		执行CRUD操作
	
	5、处理查询结果集；
		如果第4步是select语句(DQL)，才有此步
	
	6、释放资源；
		关闭所有的资源
	
Java语言中的两大类线程：守护线程和用户线程；

用户线程：如main方法主线程等；

守护线程：又称为后台线程，如垃圾回收线程等；
	特点：
		1、一般守护线程是一个死循环；
		2、所有的用户线程只要结束，守护线程自动结束；
	一般用在什么地方：
		如系统每天00:00时数据自动备份，这个时候需要使用定时器，并且将定时器设置为守护线程，
		一直监视着，每到00:00就备份一次；如果所有的用户线程结束了，守护线程自动退出，没有
		必要进行数据备份了；
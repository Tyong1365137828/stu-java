表之间的关系：
    一对一
    一对多
    多对一
    多对多

注意：mybatis中存在的关系没有 多对一，mybatis将 多对一 看作 一对一 ！！！
    mybatis存在的关系：
        一对一(注意：mybatis将 多对一 也看作 一对一)
        一对多
        多对多
    举例：
        人和身份证号之间的关系就是 一对一；
        用户和订单之间的关系就是 一对多，一个用户可以对应多个订单；
        订单与用户之间的关系就是 多对一，但是注意，如果单纯的拿出一个订单，那么此订单只能对应一个用户，所以mybatis将 多对一 看做 一对一；
        教师与学生之间的关系就是 多对多；



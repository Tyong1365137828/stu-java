<?xml version="1.0" encoding="UTF-8"?>

<!-- mybatis主配置文件 -->

<configuration>
    <!-- 配置环境 -->

    <environments default="config">
        <!-- 配置mysql的环境，注意environments标签的default属性值与environment标签的id属性值保持一致 -->

        <environment id="config">
            <!-- 配置事务类型 -->
            <transactionManager type="JDBC"></transactionManager>
            <!-- 配置数据源(即数据池) -->
            <dataSource type="POOLED">
                <!-- 配置连接数据库的基本信息 -->
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/eesy_mybatis"/>
                <property name="username" value="root"/>
                <property name="password" value="072731"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 指定映射配置文件的位置，映射配置文件指的是每个dao独立的配置文件 -->
    <mappers>
        <mapper resource="edu/hebeu/dao/IUserDao.xml"></mapper><!-- xml文件方式使用此 -->
        <!--<mapper class="edu.hebeu.dao.IUserDao"></mapper>&lt;!&ndash; 注解方式使用此 &ndash;&gt;-->
    </mappers>
</configuration>